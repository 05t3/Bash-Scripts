In order to run the script, you need to have `xautomation` installed

`sudo apt install xautomation`

In order to run the script, execute:

`source install.sh`

Then invoke the `run_all` function by running:

`run_all`
